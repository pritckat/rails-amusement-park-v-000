
<%= form_for @user, url: url_for(controller: 'sessions', action: 'create') do |f| %>
<%= f.collection_select :name, :user_id, User.all, :id, :name %>
<%= f.password_field :password %>
<%= f.submit 'Sign In'%>

<%end%>
